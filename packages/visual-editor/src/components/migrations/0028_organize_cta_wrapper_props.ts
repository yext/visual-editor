import { Migration } from "../../utils/migrate.ts";

export const organizeCTAWrapperProps: Migration = {
  // this migration was superseded by other CTA changes made before
  // the slots branch was merged, so it is now a no-op
};
