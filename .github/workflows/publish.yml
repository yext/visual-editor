# This file must be .github/workflows/publish.yml for Trusted Publishing
name: Publish Package

on:
  push:
    tags:
      - "*"

permissions:
  id-token: write # Required for OIDC

jobs:
  publish:
    if: github.repository == 'yext/visual-editor'
    runs-on: ubuntu-latest
    environment: Release
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Install pnpm
        uses: pnpm/action-setup@v4.0.0

      - name: Set node version to 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          registry-url: https://registry.npmjs.org/
          cache: "pnpm"

      - name: Install deps
        run: pnpm install --ignore-scripts --frozen-lockfile

      - name: Update npm
        # npm version npm 11.5.1 or later needed for Trusted Publishing
        run: npm install -g npm@latest

      - name: Verify package version and get npm tag
        id: get_npm_tag
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          TAG=$(pnpm run ci-verify-publish "$REF_NAME" | tail -n 1)
          echo "npm_tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Publish
        env:
          NPM_TAG: ${{ steps.get_npm_tag.outputs.npm_tag }}
        run: cd packages/visual-editor && pnpm publish --access public --tag "$NPM_TAG" --no-git-checks
